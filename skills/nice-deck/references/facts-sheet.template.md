# Facts sheet — <DECK NAME>

*Copy this per deck. It is the **single source of truth** for every number, name, URL, and
command in the deck. The rule: **never invent a fact.** If it isn't here and verified, don't put
it on a slide — flag it for the author to confirm. This is the guardrail against the model
confidently fabricating a stat, a quote, or a command.*

## How to use
1. Fill in only what you can **verify** (from the author, a doc, a repo, a live page).
2. Mark anything unconfirmed as `⚠️ UNVERIFIED` and surface it — don't silently pick a value.
3. When two sources disagree, record **both** and flag the discrepancy; let the author decide.
4. The deck cites only what's in this sheet.

## Numbers / metrics
| Metric | Value | Source | Verified? |
|---|---|---|---|
| e.g. daily active users | 0 | | ⚠️ UNVERIFIED |
| | | | |

## Names / entities
- Product / project name (exact casing):
- Org / owner:
- People (name → role, exact spelling):

## URLs (exact — these go in as native text, never AI-rendered)
- Repo:
- Product / landing page:
- Docs:

## Commands (exact — native text only)
- Install:
- Run:

## Quotes (verbatim + attribution)
- "" — , source:

## Open discrepancies to surface (not silently resolve)
- e.g. live page says X tools / repo says Y → decide with author.

## Do-not-fabricate list (things the model must not guess)
- Exact install/package name
- Any specific metric not in the table above
- Any person's title or team
- Any URL or command not listed above
